Algoritmo "velovidade"

Var
      km, ms, vm : real


Inicio
       escreva("escreva o tempo em km")
       leia(km)
       vm <- (434.4/km)
       ms <- (km/ 1.609)

       escreval("o tempo a ser percorrido ate o rio de janeiro é", vm,"hs")
       escreval("velovidade ms", ms)



Fimalgoritmo